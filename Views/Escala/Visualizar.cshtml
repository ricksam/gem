@model GEM.Models.EscalaBuilder
@{
    //Layout = null;
}

<!--div class="row noprint">
    <div class="col-md-12">
        <a class="btn btn-secondary" href="#" onclick="window.print()"><i class="fa fa-print"></i> Imprimir</a>
        <a class="btn btn-secondary" href="/Escala/Index"><i class="fa fa-back"></i> Voltar</a>
    </div>
</div-->

<div class="card">
    <div class="card-body">
        <div><h4 class="text-center">CONGREGAÇÃO CRISTÃ NO BRASIL</h4>
             <h5 class="text-center">@(Model.Titulo)</h5></div>

        <div class="row">

            @foreach (var item in Model.ListaEscala().GroupBy(q => q.Data.Month).ToList())
            {
                @await Html.PartialAsync("~/Views/Escala/VisualizarMes.cshtml", Model.BuildMes(item.Key))
            }
        </div>
    </div>
</div>

@section Styles{
    <style type="text/css">
    hr{
        border: dotted 1px #ccc;
        margin: 3px 0;
    }
    .table td, .table th {
        font-size: 8pt;
    }

</style>


<!--style type="text/css" media="print">
    .table td {
        font-size: 8pt;
    }

    #filtros {
    }

    #voltar {
        position: absolute;
        height: 0px;
        width: 0px;
    }
</style-->
}